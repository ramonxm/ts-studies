// Primitive Types

// Defining variable in string type
const name: string = "Ramon";

// Defining variable in number type
const age: number = 23;

// Defining variable in boolean type
const programmer: boolean = true;

// Defining function with string parameter and returns void
function sayHello(name: string): void {
  console.log(`Hello, i'm ${name}`);
}

// Defining class and assigning name property as string
class Person {
  name: string = "Ramon";
}
